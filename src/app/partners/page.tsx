import type { Metadata } from "next"
import Link from "next/link"
import { Check } from "lucide-react"
import { MarketingFooter, MarketingNav } from "@/components/marketing/site"
import { Button } from "@/components/ui/button"
export const metadata: Metadata = { title: "Overdue partner program for accountants and bookkeepers", description: "Help your clients recover overdue invoices with a clear, owner-controlled workflow." }
export default function PartnersPage() { return <div className="min-h-screen bg-paper"><MarketingNav /><main className="mx-auto max-w-5xl px-5 py-20"><div className="font-mono text-[11px] uppercase tracking-[0.16em] text-moss">For accountants and bookkeepers</div><h1 className="mt-4 max-w-3xl font-display text-5xl tracking-tight text-ink sm:text-6xl">Give every client a calmer way to follow up.</h1><p className="mt-6 max-w-2xl text-lg leading-relaxed text-ink-soft">Join the early partner programme. Bring your clients into a repeatable recovery workflow and earn a referral reward when they become paid accounts.</p><div className="mt-10 grid gap-4 sm:grid-cols-3">{["Client-ready CSV setup", "Shared recovery visibility", "Partner referral reward"].map((item) => <div key={item} className="rounded-xl border border-hairline bg-surface p-5 shadow-ledger"><Check className="h-5 w-5 text-moss" /><p className="mt-4 text-sm text-ink-soft">{item}</p></div>)}</div><div className="mt-10 flex flex-wrap gap-3"><a href="mailto:hello@getoverdue.online?subject=Partner%20programme"><Button size="lg">Talk about partnering</Button></a><Link href="/signup"><Button size="lg" variant="outline">Try it first</Button></Link></div><p className="mt-6 max-w-xl font-mono text-[11px] leading-relaxed text-faint">Referral terms are being finalized with pilot partners. We will confirm the reward and attribution window before publishing it as a formal offer.</p></main><MarketingFooter /></div> }
